import React from "react";

const Connect = () => {
  return (
    <a className="px-3 py-1 rounded-lg bg-white text-black cursor-pointer select-none">
      Connect
    </a>
  );
};

export default Connect;
